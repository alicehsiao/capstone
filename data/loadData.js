import path from 'path';
import dotenv from 'dotenv';
dotenv.config({
    path: path.join(__dirname, '/../.env')
});

// This is the file system to help with reading files
import fs from 'fs';

// Database

// Import Models

